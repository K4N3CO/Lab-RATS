services:
  - type: web
    name: labrats-c2-hub
    env: node
    plan: free
    rootDir: c2-server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
