services:
  - type: web
    name: labrats-c2-hub
    env: node
    plan: free
    buildCommand: cd c2-server && npm install
    startCommand: cd c2-server && node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
